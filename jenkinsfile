pipeline{
    agent any
    stages{
        stage("terraform plan"){
            steps{
                sh " terraform init "
                sh "terraform validate"
                sh "terraform plan" 

            }
        }
    }
}